<Window x:Class="Watcher.CurrentDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Watcher"
        mc:Ignorable="d"
        Icon="pack://application:,,,/icons/icon.ico"
        Title="Состояние" Height="500" Width="300"
        Style="{DynamicResource WindowStyle}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="25" Foreground="White">Текущее состояние</Label>

        <Label Grid.Row="1" Style="{DynamicResource CaptionSubThems}">Процессор</Label>
        <Label Grid.Row="2" Style="{DynamicResource CaptionSubThems}">ОЗУ</Label>
        <Label Grid.Row="3" Style="{DynamicResource CaptionSubThems}">Сеть</Label>
        <Label Grid.Row="4" Style="{DynamicResource CaptionSubThems}">Диск</Label>

        <TextBlock Style="{DynamicResource CaptionData}" Grid.Row="1" Grid.Column="1" Text="3"/>
        <TextBlock Style="{DynamicResource CaptionData}" Grid.Row="2" Grid.Column="1" Text="3"/>
        <TextBlock Style="{DynamicResource CaptionData}" Grid.Row="3" Grid.Column="1" Text="3"/>
        <TextBlock Style="{DynamicResource CaptionData}" Grid.Row="4" Grid.Column="1" Text="3"/>

        <Button Style="{DynamicResource SubButton}" Grid.Row="5" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" x:Name="closeBtn" Click="CloseBtn_Click">ОК</Button>
    </Grid>
</Window>
